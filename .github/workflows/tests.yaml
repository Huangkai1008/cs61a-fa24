name: Test

on: [push]

jobs:
  build:
    name: ${{ matrix.os }} / ${{ matrix.python-version }}
    runs-on: ${{ matrix.image }}
    strategy:
      matrix:
        os: [ Ubuntu, macOS]
        python-version: ["3.9", "3.10", "3.11" ]
        include:
          - os: Ubuntu
            image: ubuntu-22.04
          - os: macOS
            image: macos-12
      fail-fast: false

    steps:
      - uses: actions/checkout@v3
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          uv sync --all-groups
      - name: Lint
        run: |
          # uv run mypy .
          uv run ruff check .
          uv run ruff format --check .
      - name: Test
        run: |
          find hw lab project -type f -name "*.py" ! -name "__*" ! -name "construct_check.py" ! -name "utils.py" ! -name "ucb.py" ! -path "*/tests/*" ! -path "*/gui_files/*" ! -path "*/__pycache__/*" -exec uv run python -m doctest -v {} +
